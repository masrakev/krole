fastapi==0.115.9
uvicorn[standard]==0.34.0
httpx==0.28.1
pydantic==2.10.4
# 2.14.1 : aligné sur ce qu'exige la stack langchain/ragas (rétro-compatible).
pydantic-settings==2.14.1
python-multipart==0.0.20
pypdf==5.1.0
python-docx==1.1.2
# 0.3.11 : remonté par langchain 0.3.x (tiré par ragas) ; API inchangée.
langchain-text-splitters==0.3.11
chromadb==1.0.0
rank-bm25==0.2.2
FlagEmbedding==1.3.4
# FlagEmbedding 1.3.4 importe is_torch_fx_available, retiré dans transformers 5.x.
transformers==4.46.3
# Mode vocal local : STT (Whisper via CTranslate2) + TTS (Piper).
faster-whisper==1.1.0
piper-tts==1.2.0
# Évaluation 100 % locale : RAGAS + juge/embeddings Ollama (PAS d'OpenAI).
ragas==0.2.15
langchain-ollama==0.2.2
# Fournisseur cloud OPTIONNEL pour la génération (LLM_PROVIDER=claude).
# Jamais appelé en profil par défaut (mistral local). Gemini passe par httpx.
anthropic==0.109.1
